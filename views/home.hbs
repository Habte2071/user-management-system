<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Management System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
        body {
            background: linear-gradient(135deg, #6a11cb, #2575fc); /* Purple to blue gradient */
            color: #ffffff; /* White text */
            font-family: 'Arial', sans-serif; /* Modern font */
        }

        .container {
            margin-top: 50px; /* Space from top */
            background-color: rgba(255, 255, 255, 0.2); /* Semi-transparent white background */
            border-radius: 20px;
            padding: 30px;
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.3);
            transition: transform 0.5s; /* Smooth scale transition on container */
        }

        .container:hover {
            transform: translateY(-5px); /* Slight lift on hover */
            box-shadow: 0 25px 70px rgba(0, 0, 0, 0.5); /* Enhanced shadow */
        }

        h1 {
            color: #ffdd57; /* Bright yellow heading */
            text-align: center; /* Centered heading */
            margin-bottom: 20px; /* Space below heading */
            font-size: 2.5rem; /* Larger font size */
            animation: fadeIn 1s; /* Animation for heading */
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        .table {
            margin-top: 20px;
            border-collapse: collapse; /* Ensure borders collapse into one */
        }

        .table th, .table td {
            border: 2px solid #ffffff; /* White border for table cells */
            transition: background-color 0.3s; /* Smooth background color transition */
            border-radius: 10px; /* Rounded corners for cells */
        }

        .table th {
            background-color: #4e54c8; /* Dark blue for header */
            text-align: center; /* Centered text in header */
        }

        .table-striped tbody tr:nth-of-type(odd) {
            background-color: #2b5876; /* Dark teal for odd rows */
        }

        .table-striped tbody tr:nth-of-type(even) {
            background-color: #4eCDC4; /* Light teal for even rows */
        }

        .table-striped tbody tr:hover {
            background-color: #ff6b6b; /* Highlighted color on row hover */
            color: #ffffff; /* Ensure text is white on hover */
        }

        .btn-animated {
            position: relative;
            overflow: hidden;
            transition: background-color 0.4s, transform 0.3s; /* Smooth transition */
            border-radius: 50px; /* Rounded buttons */
        }

        .btn-animated::after {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 300%;
            height: 300%;
            background: rgba(255, 255, 255, 0.2);
            transform: translate(-50%, -50%) scale(0);
            border-radius: 50%;
            transition: transform 0.5s;
            z-index: 0;
        }

        .btn-animated:hover::after {
            transform: translate(-50%, -50%) scale(1);
        }

        .btn-animated:hover {
            background-color: #1abc9c; /* Bright teal on hover */
            color: white; /* Change text color on hover */
            transform: scale(1.1); /* Slightly enlarge the button */
        }

        .btn-primary {
            background-color: #3498db; /* Bright blue button color */
            border: none; /* Remove border */
            border-radius: 50px; /* Rounded buttons */
        }

        .btn-primary:hover {
            background-color: #2980b9; /* Darker blue on hover */
        }

        @media (max-width: 768px) {
            .container {
                padding: 20px; /* Reduce padding on smaller screens */
            }

            h1 {
                font-size: 2rem; /* Smaller heading on mobile */
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="row mb-3">
            <div class="col-6">
                <h1>Users</h1>
            </div>
            <div class="col-6 text-end">
                <a href="./adduser" type="button" class="btn btn-primary align-self-center">+ ADD USER</a>
            </div>
        </div>
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">First Name</th>
                    <th scope="col">Last Name</th>
                    <th scope="col">Email</th>
                    <th scope="col">Phone</th>
                    <th scope="col">Action</th>
                </tr>
            </thead>
            <tbody>
                {{#each rows}}
                <tr>
                    <th scope="row">{{this.id}}</th>
                    <td>{{this.first_name}}</td>
                    <td>{{this.last_name}}</td>
                    <td>{{this.email}}</td>
                    <td>{{this.phone}}</td>
                    <td>
                        <a href="#" class="btn btn-primary btn-animated btn-sm me-2">
                            <i class="bi bi-eye"></i> View
                        </a>
                        <a href="/edituser/{{this.id}}" class="btn btn-warning btn-animated btn-sm me-2">
                            <i class="bi bi-pencil"></i> Edit
                        </a>
                        <a href="/{{this.id}}" class="btn btn-danger btn-animated btn-sm">
                            <i class="bi bi-person-x"></i> Delete
                        </a>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>

</html>