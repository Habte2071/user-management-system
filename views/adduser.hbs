<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Management System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body {
            background: linear-gradient(135deg, #6a11cb, #2575fc); /* Purple to blue gradient */
            color: #ffffff; /* White text */
            font-family: 'Arial', sans-serif; /* Modern font */
        }

        .container {
            max-width: 600px; /* Adjusted for wider content */
            width: 100%; /* Full width on small screens */
            padding: 30px;
            background: rgba(255, 255, 255, 0.1); /* Semi-transparent white background */
            border-radius: 15px;
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.5);
            transition: transform 0.3s; /* Smooth scaling on hover */
            margin: 50px auto; /* Center the container */
        }

        .container:hover {
            transform: scale(1.05); /* Slightly enlarge on hover */
        }

        h1 {
            color: #ffdd57; /* Bright yellow heading */
            margin-bottom: 20px;
            font-weight: 600; /* Bold font weight */
            text-align: center; /* Centered heading */
            animation: fadeIn 1s; /* Animation for heading */
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        .form-floating {
            margin-bottom: 20px;
        }

        .form-control {
            border-radius: 5px; /* Rounded inputs */
            border: 2px solid #004080; /* Blue border */
            background-color: #f0f8ff; /* Light blue input background */
            color: #333; /* Dark text in inputs */
            transition: border-color 0.3s, box-shadow 0.3s; /* Smooth transition */
            padding-top: 16px; /* Prevent overlap with placeholder */
        }

        .form-control:focus {
            border-color: #003366; /* Darker blue on focus */
            box-shadow: 0 0 0.2rem rgba(0, 51, 102, 0.25); /* Shadow effect */
            outline: none; /* Remove default outline */
        }

        .form-floating > label {
            transition: 0.2s ease all; /* Label transition */
            color: #777; /* Lighter label color */
        }

        .form-control:focus ~ label,
        .form-control:not(:placeholder-shown) ~ label {
            transform: translateY(-20px);
            font-size: 12px;
            color: #003366; /* Bright color when focused */
        }

        .btn-primary {
            background-color: #004080; /* Vibrant button color */
            border: none; /* Remove border */
            border-radius: 5px; /* Rounded button */
            transition: background-color 0.3s, transform 0.3s; /* Button transition */
            font-weight: bold; /* Bold button text */
        }

        .btn-primary:hover {
            background-color: #003366; /* Darker blue on hover */
            transform: scale(1.05); /* Slightly enlarge the button on hover */
        }

        @media (max-width: 768px) {
            .container {
                padding: 20px; /* Reduce padding on smaller screens */
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>User Management</h1>
    
        <form id="addUserForm" method="POST" action="/adduser/{{this.id}}">
       {{> userform}}
        </form>
    
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>